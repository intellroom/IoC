# Criptomineria — índice

**TLP:CLEAR**

- **[`05012023.txt`](05012023.txt)** (05-01-2023) — indicadores de un cryptominer con varios módulos Python (`modularseven`, `driftme`, `catme`), hash de `unmi.sh`, y una dirección de wallet Stellar (XLM) usada como identificador de worker.
- **[`GHOSTENGINE/`](GHOSTENGINE/)** — campaña GhostEngine, con su propio README.
